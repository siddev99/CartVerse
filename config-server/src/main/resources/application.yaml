server:
  port: 7072

spring:
  application:
    name: config-server
  profiles:
    active: native

eureka:
  client:
    service-url:
      defaultZone: ${EUREKA_SERVER_ADDRESS:http://localhost:8761/eureka/}


