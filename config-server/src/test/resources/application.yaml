server:
  port: 7072

spring:
  application:
    name: config-server
  profiles:
    active: native
  cloud:
    discovery:
      enabled: false
    netflix:
      eureka:
        client:
          enabled: false